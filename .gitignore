# Windows / OneDrive
Thumbs.db
desktop.ini
ehthumbs.db
$RECYCLE.BIN/

# Editors
.vscode/
.idea/
*.swp

# Node
node_modules/
npm-debug.log*

# Secrets — never commit. Supabase anon key is public, but keep keys out of git.
.env
.env.*
!.env.example
